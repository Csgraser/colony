import React from 'react';
// import playerImage from `${colonists[0].photo}`;

export default function Timer(props) {
	return (
		<div className="Timer">
			<span className="TimerText">Timer Will Go Here</span>
		</div>
	)
}